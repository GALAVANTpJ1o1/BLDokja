# legacy/ is a verbatim, hash-pinned copy of the old project (see docs/AUDIT.md).
# Disable line-ending conversion so checkouts stay byte-identical to the originals.
legacy/** -text

# Generated alg datasets: LF everywhere, so `pnpm engine:generate --check` compares the exact bytes.
content/algs/** text eol=lf
