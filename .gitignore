# Personal data — the legacy letter-pair database and any copies must never be committed
*.db
*.db-*
*.sqlite
*.sqlite3
*.sqlite-*

# Secrets
.env
.env.*

# Local Claude Code settings
.claude/settings.local.json

# Local agent instructions supplied outside the repository
AGENTS.md
FRONTEND (1).md

# Legacy Android build output, IDE state and machine-specific paths
legacy/android/**/build/
legacy/android/.gradle/
legacy/android/.idea/
legacy/android/local.properties

# Node / build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# OS / editor
Thumbs.db
.DS_Store

# Next.js
.next/
out/
next-env.d.ts
.artifacts/
.impeccable/review/
.impeccable/mocks/
.impeccable/config.local.json
apps/web/public/workers/
apps/web/public/vendor/cubing/
apps/web/public/fonts/recursive-*.woff2

# Exported personal data (legacy import output, backups) — never commit
*.export*.json
