---
name: BLDokja
description: Exact cubes and spatial letters in a readable, optionally scenic workbench.
colors:
  slate-light-ground: "#E8E7EF"
  slate-light-stage: "#DCDAE6"
  slate-light-text: "#23253A"
  slate-light-text-quiet: "#4E5068"
  slate-light-rule: "#A9A7BA"
  slate-light-accent: "#615584"
  slate-dark-ground: "#2C2F45"
  slate-dark-stage: "#34374F"
  slate-dark-text: "#EFE9E1"
  slate-dark-text-quiet: "#B9B6C4"
  slate-dark-rule: "#4F5474"
  slate-dark-accent: "#C4B7E2"
  jade-light-ground: "#E1EEEA"
  jade-light-stage: "#CEE3DB"
  jade-light-text: "#173D36"
  jade-light-text-quiet: "#3C6258"
  jade-light-rule: "#789E91"
  jade-light-accent: "#17675E"
  jade-dark-ground: "#173B3D"
  jade-dark-stage: "#21494A"
  jade-dark-text: "#EFF7F3"
  jade-dark-text-quiet: "#B5D3CB"
  jade-dark-rule: "#628B80"
  jade-dark-accent: "#9AD8C7"
  coral-light-ground: "#F4E7E5"
  coral-light-stage: "#ECD2CE"
  coral-light-text: "#512F3C"
  coral-light-text-quiet: "#77505F"
  coral-light-rule: "#B88791"
  coral-light-accent: "#9B4459"
  coral-dark-ground: "#493244"
  coral-dark-stage: "#583E4F"
  coral-dark-text: "#FFF0E8"
  coral-dark-text-quiet: "#E2C1C7"
  coral-dark-rule: "#A97C88"
  coral-dark-accent: "#EFAAA0"
  cotton-light-ground: "#EDE8F6"
  cotton-light-stage: "#DFD5F0"
  cotton-light-text: "#373150"
  cotton-light-text-quiet: "#665879"
  cotton-light-rule: "#A69CBD"
  cotton-light-accent: "#69578B"
  cotton-dark-ground: "#36324E"
  cotton-dark-stage: "#433C5E"
  cotton-dark-text: "#F8EFFB"
  cotton-dark-text-quiet: "#CDC1E0"
  cotton-dark-rule: "#89809F"
  cotton-dark-accent: "#C7AADB"
  ocean-light-ground: "#E0ECF4"
  ocean-light-stage: "#CCDFED"
  ocean-light-text: "#1A3652"
  ocean-light-text-quiet: "#446079"
  ocean-light-rule: "#819BB2"
  ocean-light-accent: "#285F91"
  ocean-dark-ground: "#24364F"
  ocean-dark-stage: "#2E4460"
  ocean-dark-text: "#EEF5FE"
  ocean-dark-text-quiet: "#BDD0E9"
  ocean-dark-rule: "#7087A2"
  ocean-dark-accent: "#9CC7EE"
  forest-light-ground: "#E7EDDF"
  forest-light-stage: "#D8E2CB"
  forest-light-text: "#2D3F30"
  forest-light-text-quiet: "#526650"
  forest-light-rule: "#879C79"
  forest-light-accent: "#476A4C"
  forest-dark-ground: "#2E3F37"
  forest-dark-stage: "#394E43"
  forest-dark-text: "#F2F4E8"
  forest-dark-text-quiet: "#C4D1BC"
  forest-dark-rule: "#80957A"
  forest-dark-accent: "#BAD0A8"
  standard-face-u: "#F4F4F1"
  standard-face-f: "#1FA25A"
  standard-face-r: "#D63A3A"
  standard-face-d: "#FFD23F"
  standard-face-l: "#F57C1F"
  standard-face-b: "#2B6CD9"
  high-contrast-face-u: "#FFFFFF"
  high-contrast-face-f: "#00B050"
  high-contrast-face-r: "#E00000"
  high-contrast-face-d: "#FFEE00"
  high-contrast-face-l: "#FF8000"
  high-contrast-face-b: "#0047FF"
  deuteranopia-face-u: "#FFFFFF"
  deuteranopia-face-f: "#009E73"
  deuteranopia-face-r: "#D55E00"
  deuteranopia-face-d: "#F0E442"
  deuteranopia-face-l: "#CC79A7"
  deuteranopia-face-b: "#0072B2"
  cube-body: "#0E0E10"
typography:
  display-letter:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "3rem"
    fontWeight: 700
    lineHeight: 1
    fontVariation: '"CASL" 1, "MONO" 0'
  hero:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "clamp(2.7rem, 5vw, 4.8rem)"
    fontWeight: 720
    lineHeight: 1.04
    letterSpacing: "-0.055em"
  title:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "clamp(2rem, 3.5vw, 3.25rem)"
    fontWeight: 680
    lineHeight: 1.12
    letterSpacing: "-0.045em"
  heading:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "1.375rem"
    fontWeight: 620
    lineHeight: 1.25
  subheading:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "1.125rem"
    fontWeight: 600
    lineHeight: 1.35
  body:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "1.0625rem"
    fontWeight: 420
    lineHeight: 1.6
    fontVariation: '"CASL" 0, "MONO" 0'
  ui:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "0.9375rem"
    fontWeight: 500
    lineHeight: 1.4
  meta:
    fontFamily: '"Recursive Casual", "Recursive Casual Fallback", ui-sans-serif, system-ui, sans-serif'
    fontSize: "0.8125rem"
    fontWeight: 450
    lineHeight: 1.4
  notation:
    fontFamily: '"Recursive Mono", "Recursive Mono Fallback", ui-monospace, monospace'
    fontSize: "1rem"
    fontWeight: 520
    lineHeight: 1.5
    fontVariation: '"CASL" 0, "MONO" 1'
rounded:
  letter-tile: "4px"
  mark: "2px"
  button-field: "10px"
  appearance-choice: "12px"
  panel: "14px"
  tool-link: "16px"
  cube-stage: "18px"
  shell: "24px"
  hero-stage: "28px"
  transmission: "0px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "24px"
  2xl: "32px"
  3xl: "48px"
  4xl: "64px"
components:
  button:
    backgroundColor: "transparent"
    textColor: "{colors.slate-light-text}"
    rounded: "{rounded.button-field}"
    padding: "0 1rem"
    height: "44px"
  button-hover:
    backgroundColor: "{colors.slate-light-stage}"
  button-strong:
    backgroundColor: "{colors.slate-light-text}"
    textColor: "{colors.slate-light-ground}"
    rounded: "{rounded.button-field}"
    padding: "0 1rem"
    height: "44px"
  field:
    backgroundColor: "{colors.slate-light-ground}"
    textColor: "{colors.slate-light-text}"
    rounded: "{rounded.button-field}"
    padding: "0 0.75rem"
    height: "44px"
  panel:
    backgroundColor: "color-mix(in srgb, #DCDAE6 24%, #E8E7EF)"
    rounded: "{rounded.panel}"
  appearance-choice:
    rounded: "{rounded.appearance-choice}"
    padding: "0.65rem"
  nav-active:
    backgroundColor: "{colors.slate-light-stage}"
    textColor: "{colors.slate-light-text}"
    rounded: "{rounded.button-field}"
    padding: "0.65rem 1rem"
  transmission:
    backgroundColor: "color-mix(in srgb, #E8E7EF 92%, #23253A)"
    textColor: "{colors.slate-light-text}"
    rounded: "{rounded.transmission}"
    padding: "1.5rem"
---

> **Current authority — September 17, 2026.** The frontmatter records actual implemented tokens; theme-qualified colour keys describe the six selectable interface colourways, each with independently designed light and dark values. Runtime sources are `apps/web/src/design/appearance.ts`, `apps/web/src/styles/colourways.css`, `apps/web/src/design/palette.ts`, `apps/web/src/styles/tokens.css` and `apps/web/src/app/globals.css`. Each colourway's focus colour equals its text colour. Component frontmatter values show Slate studio in light tone; live component snippets in `.impeccable/design.json` bind to the active CSS properties. Recorded `44px` component heights are minimum heights, not fixed clipping boxes.

> The historical prose below is retained. The September 17 extension at the end supersedes earlier **no-interface-accent / cube-only hue** and **light-theme-no-scenery** statements only for the surrounding room's decoration. It does not weaken the rule that a semantic face colour always means its face, nor permit theme colours to label cube faces, tracks, correctness or mastery. The September 16 top-navigation and revised-radius decisions also supersede the original rail and universal-4px layout proposal.

> This is an approved established-system extension, not a replacement visual world. No five-block direction record, image-comp approval, concept roll, signature name or selection seed is claimed.

# BLDokja design system

Design tokens and the reasoning behind them (BRIEF §9), combined with the constellation direction in `DESIGN-DIRECTION.md`. The base system comes from your answers on 2026-09-15; the constellation layer (starfield, page transitions, transmission window, letters as stars) is added on top of it, as DESIGN-DIRECTION.md §6 asks. Where I had to fill a gap or reconcile the two, it's named below and in `docs/OVERNIGHT.md`. Tokens live in `apps/web/src/styles/tokens.css`, and `/lab` shows every one of them.

## The idea underneath

Tracing a memo is drawing a path through fixed points in a fixed order. So the site's second layer of meaning is a night sky:

- a **sticker or letter** is a star;
- a **trace** is the line drawn from star to star as each target is shot;
- a **cycle** is a constellation closing into a shape;
- the **buffer** is the anchor star everything returns to.

The persistent starfield is the quiet version of this on every page. The loud version, where guided trace draws targets as points and a completed cycle closes into a visible shape, is a Phase 4 stretch goal. Nothing in this system should fight it.

## Four principles

1. **The cube is the only colour.** The six face colours are the only hues on the site, and **a colour always means its face**. A red mark says "this is on R". Everything else is slate, chalk and ink, the starfield and its glows included. Tracks, piece types and states are told apart by type, shape, position and tone, never by a colour of their own.
2. **Letters are places, and places are stars.** A letter is a sticker. Every letter the site shows can say where it lives: the live target sits on a tile in its sticker's colour, and letters elsewhere carry a small notch in their face colour. On the starfield, a letter can also be drawn as a star (chalk with a soft glow) to mark a completion or a target.
3. **Moves only when you do.** Nothing animates on a timer or on load. Motion happens only in response to what you do: a comm turning, a target advancing, a dialog opening where you asked for it, the stars drifting as you scroll, the sky rushing past as you move to another page. Motion that carries information (the cube) is primary; the atmospheric kind (parallax, page travel) stays small, short and optional.
4. **Everything collapses to still.** Under `prefers-reduced-motion`, nothing moves: no parallax, no page travel (a plain cross-fade instead), no dialog entrance, and cube animations jump to their end with stepping. This is checked in script and in tests, not left to a media query.

## Where this departs from the brief

BRIEF §9 suggests mapping *piece types, tracks and states* to face colours. That collides with the cube: on a screen with a cube, a red "corners" label would read as "the R face". So colour means one thing only, the face, and the rest of the information architecture is carried by type and layout. You chose this on 2026-09-15.

## Reconciling the constellation direction

DESIGN-DIRECTION.md extends this system; where the two touch, this is how they fit:

| DESIGN-DIRECTION.md says | Existing system | Combined |
|---|---|---|
| Stars "white/accent-tinted"; windows have a "glowing border in the accent colour" | There is no accent colour; colour means a face (principle 1) | Stars and glows are **chalk** (the warm off-white text colour) in the dim-room theme, **ink** where a window shows in daylight. No new hue. |
| §4: sticker colours stay "the primary semantic language across the UI (piece types, tracks, states)" | Your 2026-09-15 answer: colour means only its face | The face-only rule stands; DESIGN-DIRECTION.md §6 says to keep what's already decided. |
| Parallax on scroll, page travel, dialog entrance | "Still until touched"; CLAUDE.md: motion only in response to user action | Principle 3 is restated as "moves only when you do". All three respond to something you did (scroll, navigate, open) and never run on their own. |
| Angular transmission windows | Sticker grid: 4px radii | The transmission window is the one deliberately angular surface, so dialogs read as a different layer from the page. |
| Starfield in light mode: off or near zero | Daylight is a designed theme, not an inversion | The starfield isn't drawn in the light theme at all. |
| No references to any specific work | — | The HUD and starfield vocabulary stays generic; copy in windows is plain sentences. |

## Colour

### Interface colours

Six named colours, two themes. Neither theme is an inversion of the other: the light theme is the same room in daylight, with its own contrast decisions (for example, borders are darker in light, and the stage is lighter than the ground in dark but darker in light).

| Token | Role | Dim room (dark) | Daylight (light) |
|---|---|---|---|
| `--ground` | Page background | **Slate** `#2C2F45` | **Lilac mist** `#E8E7EF` |
| `--stage` | Behind the cube and the letter grid | `#34374F` | `#DCDAE6` |
| `--text` | Body text, letters | **Chalk** `#EFE9E1` | **Ink** `#23253A` |
| `--text-quiet` | Secondary text, labels | `#B9B6C4` | `#4E5068` |
| `--rule` | Borders, dividers, grid lines | `#4F5474` | `#A9A7BA` |
| `--focus` | Keyboard focus ring (2px, offset 2px) | Chalk | Ink |

- **No accent colour.** Links are underlined, buttons have a border and a heavier weight, the current nav item gets a solid bar. An accent hue would compete with the stickers and break principle 1.
- **Contrast (WCAG 2.x), checked in `apps/web/src/design/palette.test.ts`:**

  | Pair | Dark | Light | Floor |
  |---|---|---|---|
  | Text on ground | ≥ 7:1 | ≥ 7:1 | 7 (AAA) |
  | Quiet text on ground | ≥ 4.5:1 | ≥ 4.5:1 | 4.5 (AA) |
  | Text on stage | ≥ 4.5:1 | ≥ 4.5:1 | 4.5 |
  | Focus ring on ground | ≥ 3:1 | ≥ 3:1 | 3 (non-text) |
  | Rule on ground | ≥ 1.5:1 | ≥ 1.5:1 | a visible hairline |

  The test prints the exact ratios.

### Sticker palettes

Face colours are their own token set, `--face-u` … `--face-b`, switched as a whole by the palette setting. The same tokens drive the 3D cube, the 2D nets, letter tiles and notches, so a colourblind preset recolours every face colour on the site.

| Palette | U | F | R | D | L | B |
|---|---|---|---|---|---|---|
| **Standard** | `#F4F4F1` | `#1FA25A` | `#D63A3A` | `#FFD23F` | `#F57C1F` | `#2B6CD9` |
| **High contrast** | `#FFFFFF` | `#00B050` | `#E00000` | `#FFEE00` | `#FF8000` | `#0047FF` |
| **Deuteranopia-safe** | `#FFFFFF` | `#009E73` | `#D55E00` | `#F0E442` | `#CC79A7` | `#0072B2` |

- **Deuteranopia-safe** takes the Okabe–Ito colours. Red and green are replaced by vermillion and bluish green, and orange (which collapses into red for deuteranopes) becomes reddish purple. The letters stay on their usual faces; only the colours change.
- **The cube body is black** (`#0E0E10`) in every theme. It keeps white and yellow stickers readable on lilac mist and blue stickers readable on slate, the lowest-contrast pairs.
- **A tile's letter is ink or chalk,** whichever contrasts more with that face colour. Every face in every palette reaches at least 3:1, the WCAG floor for large text, which a 48px bold tile letter is. In the standard palette white, green, yellow and orange take ink; red and blue take chalk. The test checks every palette.

### States without colour

| State | Shown as |
|---|---|
| Correct | "Correct" in text, a check-shaped mark, the item settles in place |
| Wrong | "Not quite: it's M" in text, a cross-shaped mark, the right sticker highlighted on the cube |
| Due, new, mastered | a word label, plus a mark: an open ring (due), a dot (new), a filled square (mastered) |
| Mastery grids | one lightness ramp in `--text` at 5 steps (12% to 100% opacity), plus the marks above |
| Current nav item | a solid bar on the leading edge and a heavier weight |

## Type

**Recursive**, one variable family, self-hosted (`@fontsource-variable/recursive`), with its axes doing the work of three typefaces:

- **Linear sans** (`CASL 0, MONO 0`) for all reading text.
- **Casual** (`CASL 1`) only for big single letters: the live target, section letters in the lettering trainer. Its uppercase forms are distinctive enough to act as an element on their own.
- **Mono** (`MONO 1`) for alg notation, letter pairs in grids and memo lines, where columns must line up.

| Role | Size / line height | Settings | Used for |
|---|---|---|---|
| `display-letter` | 3rem / 1 | wght 700, CASL 1 | The live target tile (72px tile) |
| `title` | 2rem / 1.15 | wght 650 | Page titles |
| `heading` | 1.375rem / 1.25 | wght 620 | Section headings |
| `subheading` | 1.125rem / 1.35 | wght 600 | Sub-sections, panel titles |
| `body` | 1.0625rem / 1.6 | wght 420 | Lessons and descriptions |
| `ui` | 0.9375rem / 1.4 | wght 500 | Buttons, inputs, nav |
| `meta` | 0.8125rem / 1.4 | wght 450 | Captions, counts, hints |
| `notation` | 1rem / 1.5 | wght 520, MONO 1 | Algs, memo lines, pair grids |

- **Sentence case everywhere.** No tracked uppercase labels.
- **Line length:** lesson text is capped at 68ch.

## Shape and layout

**Sticker grid.** 4px corner radius on panels, buttons, inputs and tiles; 2px on small marks. Borders instead of shadows: a 1px `--rule` line, and nothing floats. Spacing is an 8px scale (`4 8 12 16 24 32 48 64`). Tiles and grid cells are square.

**The workbench.** Every page that has a cube is laid out around it.

- **Desktop (≥ 960px):** a nav rail on the left (Learn · Practice · Progress, settings at the bottom). The cube stage takes a sticky left column (min 360px); text and controls take the right.
- **Tablet (600–959px):** the rail collapses to icons with labels underneath; the stage sits above the content, at most 60% of the viewport height.
- **Phone (< 600px):** a bottom bar holds the three sections and settings. The stage is full-width and square, at least 300px of cube at 380px wide, and the controls it needs sit directly under it, within thumb reach.

Pages without a cube (the letter-pair library, progress) use the same rail and a single content column.

**Home adapts.** A new visitor lands on the learning path. Once any lesson or drill is started, home becomes *Today*: reviews due, the next lesson, the weak 20.

## The constellation layer

### Starfield (`components/starfield`)

- **One `<canvas>`** fixed behind all content: never per-star DOM nodes. It is `aria-hidden` and ignores the pointer.
- **Stars** are small points in chalk at 4–10% strength, three depth layers with slightly larger, brighter near stars. They're placed by a seeded best-candidate scatter, so the field is irregular (no grid, no clumps) and the same on every visit.
- **Parallax:** stars move only when you scroll, at 0.1×, 0.2× and 0.3× the scroll speed by layer. There is no animation loop: the canvas repaints on scroll, resize and theme change, and not otherwise.
- **Adaptive count:** one star per 9,000 px² of viewport, capped at 240 on large screens and 90 on phones, and halved again where the browser reports low memory, few CPU cores or data saving.
- **Contrast by construction:** each star is painted opaque, in the ground colour blended toward chalk by at most 10%, onto a canvas filled with the ground. Overlapping stars overwrite instead of adding up, so no pixel is brighter than one star at 10%. `starfield.test.ts` checks body text, quiet text and the focus ring against that brightest colour (brightest star colour `#404255`: text 8.2:1, quiet text 5.0:1, focus ring 8.2:1; the test prints the ratios).
- **Light theme:** not drawn.
- **Reduced motion:** scrolling isn't even listened to; the field is painted once and stays put. A test paints the whole field at two scroll positions and requires identical output.

### Moving between pages (`components/transitions`)

- **Mechanism:** the View Transitions API, through `TransitionLink` (used by the nav). The route change runs inside `document.startViewTransition`, and the kind of transition is stamped on `<html>` as `data-transition` while it runs.
- **Full motion, "travel":** the old sky streaks outward (scale to 1.08 with a faint blur) and fades; the old page recedes (scale 0.97) and fades; the new sky settles from 1.05 to rest while the new page arrives from 0.98. Nothing exceeds 110% scale, and everything finishes within 360ms.
- **Reduced motion, "fade":** a 160ms cross-fade of the whole page. Nothing scales, streaks or moves.
- **No View Transitions API:** the page just changes.
- The effect only shows once there are lessons to move between (Phase 3); the mechanism is in place now.

### Transmission window (`components/ui/transmission-window.tsx`)

- **The one dialog shell:** settings confirmations now, the first-visit voice picker and trainer dialogs later.
- **Shape:** a square-cornered panel on a slightly lifted ground, a 1px chalk border at 38% with a soft chalk glow, and small L-shaped corner brackets. The backdrop is the ground at 55% with a 6px blur, not a flat scrim.
- **Behaviour:** native `<dialog>` with `showModal()`, so focus stays inside, Escape and a backdrop click close it, and the page behind is inert.
- **Entrance:** scale from 95% and fade, 160ms. With reduced motion it simply appears.
- **Copy:** plain. A clear heading and ordinary sentences; no invented terminology, no bracketed system-message phrasing.

### Letters as stars

`LetterStar` draws a letter in chalk with a two-step soft glow, in the casual display style: for a lesson's completion mark, or a target display on the starfield. It's the literal form of principle 2.

## Motion

- **Durations:** 120ms for state changes in the interface (a mark appearing, a button state); 160ms for a dialog entrance and the reduced-motion cross-fade; up to 360ms for page travel. Cube move timing is cubing.js's, scaled by the tempo setting.
- **Easing:** `cubic-bezier(0.2, 0, 0, 1)`, a quick start that settles; page travel accelerates out and decelerates in.
- **Allowed:** the cube turning; a tile flipping to its next letter; a dialog entering where you opened it; stars drifting while you scroll; page travel when you navigate.
- **Not allowed:** anything on a timer or on load; entrance animations for page sections; hover lifts; looping decoration; skeleton shimmer; zoom beyond 110%.
- **Reduced motion, checked in script as well as by media query** (`design/motion.ts` stamps `data-motion` on `<html>`, and the CSS keys its reduced rules on both):
  - no parallax;
  - page travel becomes a cross-fade;
  - dialogs appear without an entrance;
  - cube animations jump to their end.

  `motion.test.ts` checks the script path, the transition kinds, and that every moving rule in the stylesheet has both overrides.

## Review against the brief and CLAUDE.md

| Tell to avoid | Where it could creep in | What this design does instead |
|---|---|---|
| Near-black ground with one acid accent | A dark theme | Slate is clearly blue-violet (lightness 22%), and there is no accent at all |
| Cream ground, high-contrast serif, terracotta | A light theme | Lilac mist is cool, the type is a sans, and there's no terracotta or any accent |
| Identical rounded cards with soft grey shadows | Lesson lists, dashboards | No shadows; rules and square grids; lists are rows, not cards |
| Tracked-out uppercase eyebrow labels | Section labels | Sentence case only; sections are marked by position and weight |
| `01 / 02 / 03` on non-sequences | Feature lists | Numbers appear only on real sequences: lesson order, solve steps |
| One word coloured or italicised in a headline | Landing copy | Colour means a face; headlines are plain |
| `→` on buttons | Calls to action | Verbs only: "Start lesson", "Drill these" |
| Fade-and-slide-up entrances | Every section | Principle 3: nothing moves on load |
| Space theme as decoration | A starfield on every page | The starfield means something (letters are stars, traces are lines), stays at 4–10%, moves only with scroll, and gives nothing an exemption from the rules above |
| Glowing neon HUD everywhere | Dialogs | Only the transmission window uses the HUD vocabulary, in chalk, with plain copy |

**Is anything still generic?** The slate/lilac pairing is calm but plain on its own. What makes it this site is that the only colour anywhere is a sticker, the letters act as places, and places are stars. If `/lab` looks like "a dark dashboard" or "a generic space theme", the fix is more letters, cubes and traces, not an accent.

## Accessibility floor (built in from the start)

- Every cube has a text representation: a face-by-face description and, where a trace exists, the targets in order (`CubeDescription`).
- Full keyboard operation with visible focus (the `--focus` ring above); trainer shortcuts are single keys, listed under `?`.
- Every palette is available in both themes; a palette never depends on a theme.
- Touch targets at least 44×44px on phones.
- The starfield and page transitions are `aria-hidden` and decorative; screen-reader users lose nothing without them.
- Text over the starfield keeps WCAG AA against the brightest star colour possible, checked in `starfield.test.ts`.
- Reduced motion collapses every moving part to still (principle 4), checked in `motion.test.ts` and `starfield.test.ts`.

## September 16 polish: a quieter shell, a larger cube

The redesign retains Recursive, both existing themes, the constellation layer and face-only colour semantics. It uses the redesign-existing-projects, design-taste-frontend and high-end-visual-design skills for hierarchy, spacing and restrained surfaces, not to replace the established identity.

- Desktop navigation moves to a compact top bar; phones use a bottom navigation pill. The cube occupies the largest useful area on the home page and stays alongside the active drill on wider screens.
- Secondary trainer settings and algorithm explanations become disclosures. Lessons use section rules, readable prose widths, a desktop table of contents and a saved-position resume link instead of repeated enclosing panels.
- Panels use 14px corners; cube stages and the home hero have distinct larger radii. Reference and algorithm tables remain dense, scrollable, explicit data structures rather than decorative cards.
- Button press and verified-result feedback respond to actions only. Both OS and site reduced-motion settings disable the added motion.
- Lessons and trainers initially show an exact highlighted net. Inspect in 3D enables WebGL for the current session; the home hero remains 3D. Text view supports explicit move-by-move replay. This keeps the cube present without requiring a graphics engine before reading or practising.
- On 5x5 centres, precise identity highlights use the net, not a misleading shared-colour 3D mask.

## September 17 extension: approval and evidence

The user approved richer selectable palette themes inspired by their screenshots, optional galaxy/rain/snow/forest scenery plus ocean, compact layout and a site guide. This is precise extension authority for the incumbent system, not blanket UI approval, a phase advance or an image-comp approval. `PRODUCT.md` records durable product constraints; `UX-CONTRACT.md` owns shared flow consequences. The actual tokens above are normative; the earlier sections remain an historical decision record.

The portable sidecar uses schema version 2. Its colour metadata includes derived eight-step OKLCH strips for panel previews only; these are not new runtime palette tokens. Its component samples use self-contained CSS bound to the existing active custom properties.

## Overview

**Creative North Star: "Letters are places, and places are stars."**

The established constellation metaphor remains: letters locate stickers, traces connect those places, and the exact engine-driven cube is the hero. Recursive stays the only type family. The September 17 approval extends the room around that work with selectable colourways and scenery; it does not replace the teaching interface or invent a new identity.

Reading and operating surfaces remain opaque and legible. Visitors can choose light or dark independently from a colourway, a semantic sticker palette, a scene and a comfortable or compact layout. The six-step site guide is an optional, replayable route into the existing product, not a prerequisite for using it.

Decoration is still until an action changes it. One canvas reacts to scroll, settings, resize and bounded navigation travel; reduced motion makes the scene static. No uploaded reference imagery, copied visual assets, new network dependency or idle animation loop is required.

**Key Characteristics:**

- Exact cubes and semantic face-coloured letters remain the spatial anchors.
- Six interface colourways have independent light and dark tones.
- Opaque work surfaces separate foreground contrast from optional scenery.
- Compact density preserves important control and sticker target sizes.
- A saved, replayable guide uses native modal and keyboard behaviour.

## Colors

### Primary

Each colourway's `--accent` is decorative and paired with independently chosen light and dark room colours. The picker names are **Slate studio**, **Jade lagoon**, **Coral dusk**, **Cotton skies**, **Ocean blue** and **Forest moss**. Their exact ground, stage, text, quiet-text, rule and accent values are the theme-qualified frontmatter entries extracted from `appearance.ts`; do not invent a seventh hue set or derive dark by inversion.

### Neutral

`--ground`, `--stage`, `--text`, `--text-quiet` and `--rule` continue to own the readable interface. `--focus` equals the active tone's `--text`; its visible ring remains 2px with a 2px offset. Light/dark selection supports the system preference. Defaults remain Slate studio, system tone, Standard stickers, Galaxy scenery and Comfortable layout.

The maintained three semantic sticker palettes are unchanged: Standard, High contrast and Deuteranopia-safe. Their frontmatter tokens and the historical sticker table correspond to `palette.ts` / `tokens.css`, independent of the room colourway. The cube-body token remains fixed.

**The Face Meaning Rule.** A sticker colour means its face. Interface accents decorate the room; they never redefine faces or become correctness, track or mastery codes.

`appearance.test.ts` checks every colourway in both tones: body text at least 7:1, quiet text at least 4.5:1 and focus at least 3:1 against both ground and stage, plus CSS/data agreement. This is a token-contrast check, not an assertion that arbitrary scenery pixels are safe text backgrounds.

## Typography

**Display Font:** Recursive Casual with the measured Recursive Casual Fallback, then the system sans stack.

**Body Font:** The same Recursive Casual family, with linear sans axes.

**Label/Mono Font:** Recursive Mono with Recursive Mono Fallback, then the system monospace stack.

The current role sizes, weights, leading, spacing and axes are in the frontmatter. Big spatial letters keep casual forms; algorithms and memo use mono forms. The September 16 hero and page-title scales are retained, as are sentence case and the 68ch general reading measure (lesson paragraphs use 65ch).

**Verification constraint, not design intent:** correction-stage Windows Playwright WebKit checks found that the loaded Recursive font produced the same canvas ink at weights 420 and 700, while Chromium differentiated them correctly. Keep the real CSS weights; no font substitution, synthetic fix or real-iOS approval is established by this record. Weight hierarchy still needs real-device confirmation.

## Layout

The maintained shell is the September 16 top bar, with bottom navigation below 768px, not the earlier left-rail proposal. The comfortable workbench is centred: shell main up to 1330px, workspace up to 1280px, and desktop content/control grids start at 768px. Lesson table-of-contents columns start at 1200px. Appearance choices use two columns on narrow screens and three from 600px.

Compact mode narrows shell main to 1080px and workspace/lesson article to 1024px, reduces heading and tool spacing, and keeps buttons and sticker targets unchanged. It does not create a fixed-height application shell or suppress natural document scrolling.

Dialogs contain reachable vertical overflow inside the panel, capped by dynamic viewport height minus 2rem and safe-area insets; horizontal overflow is suppressed inside that surface, with long copy allowed to wrap. Global themed platform scrollbars remain available: stable desktop gutter from 768px, thin phone scrollbars below it, and forced-colours ownership preserved. Bottom-navigation space is reserved through root scroll padding and control scroll margins.

## Elevation & Depth

The workbench uses opaque ground and tonal mixes of opaque ground/stage values. Panels are restrained rather than identical floating cards. The hero's shallow inset highlight and the mobile navigation's structural shadow are retained. The transmission window is the distinct angular modal layer: ground mixed toward text, a thin text-coloured border and soft glow, with a blurred ground-based backdrop.

Exact shadow snippets live in the sidecar because the frontmatter schema does not support shadows. Foreground contrast is isolated by opaque work surfaces, not by restricting every decorative particle to the old 10% single-star blend.

**The Opaque Work Surface Rule.** Reading, controls and cube work live on opaque ground-based surfaces; do not claim the old single-star contrast bound for the new composited scenery.

## Shapes

Keep the observed family rather than a universal radius: controls (10px), appearance choices (12px), panels (14px), tool links (16px), cube stages (18px), shell (24px) and hero stage (28px). Spatial letter tiles retain their explicit 4px corners and tiny face notches their 2px corners. Transmission windows remain square-cornered. Their 12px L-shaped brackets now sit within the panel bounds so they do not create sideways document overflow.

## Components

### Buttons

Neutral actions retain the 44px minimum-height bordered control, with a stage hover background. Strong/pressed actions invert text and ground and use stronger weight. Focus stays visibly outlined; an enabled press scales only to 0.98. Disabled buttons lower opacity to 0.5 and use the unavailable cursor. Compact mode does not shrink these controls. Guide restart now uses this same button geometry.

### Cards / Containers

Panels use a thin rule-based border and opaque tonal surface. Dense tables stay data structures with native horizontal overflow rather than decorative cards; long forms and lessons keep document scrolling. The active cube remains the largest useful teaching artifact.

### Inputs / Fields

Native fields use ground, text, a rule border, 44px minimum height and the same control corners. Labels, errors and save status stay at action scope. Radio choices remain browser-owned controls rather than clickable divs or an invented listbox.

### Navigation

Desktop top navigation and phone bottom navigation preserve visible active state and keyboard focus. Parent Back is a real route link defined by the destination's area; it is not a replacement for browser history. The guide trigger remains reachable in the header.

### Appearance choices

Colourway and scene previews are decorative and `aria-hidden`. Each selection has native radio semantics, a text label, selected outline and explicit **Selected** text. Focus is drawn around the enclosing choice. Pending storage disables the fieldset and lowers the choice opacity; error feedback preserves the old setting and allows retry. Density uses a labelled native radio group.

**The Native Choice Rule.** Colourway and scene choices are native labelled radios with an outline and visible Selected text. Pending persistence disables the group; a failure preserves the previous choice and offers retry.

### Adaptive scenery

A single fixed, `aria-hidden`, pointer-inert canvas sits behind the opaque shell. Options are Still studio (`none`), Galaxy, Rain, Snow, Forest and Ocean. Every scene can be used in either tone; light no longer implicitly disables scenery.

The controller reuses seeded best-candidate points: one point per 9000px², capped at 240 or 90 below 600px. Save-data, at most 4GB reported memory or at most four reported CPU threads halves the cap. Save-data or low reported memory additionally caps device-pixel ratio at 1 instead of 1.5 and disables canvas navigation travel; the CPU-thread hint changes count only.

Current painter values supersede the older starfield drawing model:

| Scene | Actual layers and paint strength | Scroll/navigation behaviour |
|---|---|---|
| Galaxy | Three point depths, text colour at alpha 0.30 / 0.46 / 0.62; nearby connecting lines at 0.15 | Depth scroll factors 0.08 / 0.17 / 0.26; bounded radial trails of 10 / 26 / 42px at full travel |
| Rain | Same three depths, alpha and stroke widths 0.6 / 1.0 / 1.4px | Slanted drops extend from 12 / 20 / 28px plus the bounded travel trail |
| Snow | Same depths and alpha; point radii 1.5 / 2.5 / 3.5px | The same action-driven depth positions; no idle snowfall |
| Forest | Up to eight trees per depth; alpha 0.12 / 0.19 / 0.26; middle layer uses accent | Scroll changes depth position and a bounded 16px base displacement |
| Ocean | Five alternating accent/text contour bands; alpha 0.09 / 0.125 / 0.16 / 0.195 / 0.23 | Scroll changes contour phase; no idle waves |
| Still studio | No canvas decoration | Canvas is hidden |

Repaints follow resize, settings/tone changes and deliberate scroll. Navigation may run a requestAnimationFrame sequence for at most 360ms; hidden documents cancel pending frames and travel. Reduced motion omits the scroll listener and forces scene displacement and travel to zero. No per-particle DOM, timer-driven drift, idle RAF or remote texture is introduced.

**The Action-Bounded Scene Rule.** Scenery responds to deliberate actions, never elapsed idle time. Navigation may schedule frames for at most 360ms; reduced motion eliminates scene travel and scroll movement.

### Site guide and transmission window

The guide has six text-and-destination steps: learning path, cube display, recognition/trace, skill practice, personal memo, then review/offline. It is invited on an unseen home visit and remains replayable from the header. Its saved zero-based cursor (0–5) resumes; Previous, Next, restart and Finish are explicit controls. Step content is announced politely.

The native modal makes the background inert. Close, Escape and Finish request the saved seen state before closing; failed writes retain the dialog and prior step with retry feedback. Guide writes are guarded while busy. Closing restores focus to the explicit trigger; a home invitation returns to the stable header guide control. Destination links navigate to the described area rather than pretending the guide completed it.

The corrected entrance is an **opacity-only** 160ms transition, not the historical scale-and-fade. The backdrop also fades; reduced-motion rules remove both entrances. This retains stable pointer hit-testing during open. Internal vertical overflow, inside-bound brackets and 44px restart controls are documented as implemented corrections, not proof of a completed confirmation pass.

Appearance and guide persistence fields are optional, additive extensions to the existing v1 Zod settings record. The StorageAdapter remains authoritative; the local appearance mirror is first-paint convenience only. No migration deletes or resets older preferences.

## Do's and Don'ts

### Do:

- **Do** keep interface colourway, light/dark tone, semantic sticker palette and scenery independent.
- **Do** retain the exact engine cube, Recursive role hierarchy, semantic face colours and text cube alternative.
- **Do** keep work surfaces opaque, controls keyboard-operable and important touch targets at least 44px.
- **Do** preserve a saved guide cursor, Previous/Next/restart, Escape and explicit return focus.
- **Do** reduce scene cost using available device hints and stop navigation frames when their bounded travel ends.
- **Do** distinguish implemented code, correction captures and pending reviewer or physical-device evidence.

### Don't:

- **Don't** use interface accents to encode cube faces, correctness, tracks or mastery.
- **Don't** add idle RAF, per-particle DOM nodes, looping weather, autoplay decoration or section slide-up entrances.
- **Don't** shrink buttons or sticker targets to achieve compact density.
- **Don't** apply transform-based dialog entrances that displace guide controls during hit-testing.
- **Don't** present an image comp, concept seed, font workaround or iOS approval that was never established.
- **Don't** infer physical-cube or real-device verification from emulated browser tests.

### Verification record and remaining limits

This persistence pass checked the maintained product and UX records, actual theme/data mirrors, shared CSS, font declarations, scenery controller/model/count logic, native appearance choices, guide/copy/modal and optional settings schema. It changes only `docs/DESIGN.md` and `.impeccable/design.json`; it does not certify untouched product flows.

The six required `.impeccable/review/desktop.png`, `mobile.png`, `guide-desktop.png`, `guide-mobile.png`, `speffz-desktop.png` and `speffz-mobile.png` captures have been correction-recaptured. Supplementary `mobile-chromium.png` and `guide-mobile-chromium.png` show the retained role weights without injected CSS; the required WebKit captures retain the identified Windows harness font limitation. These are implementation/review evidence, not a user-approved image comp. The reviewer identified guide hit-testing, Windows WebKit weight hierarchy, overflow, disabled-state treatment and stale documentation. Hit-testing, overflow and disabled treatments were corrected in code; this record corrects documentation. Corrected guide and appearance checks pass all four browser targets. The complete production regression passes 77 checks with three existing intentional skips; 720 unit tests pass with one skip. The same reviewer's final fix-list verdict resolves four fixes and leaves typography partial/environment-limited, disposition **fix**, pending physical Safari or macOS WebKit evidence. No correction-batch regression was identified. No claim of real iOS, Android virtual-keyboard or physical-cube verification is made here.
